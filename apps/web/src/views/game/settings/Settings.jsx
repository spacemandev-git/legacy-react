import React from 'react';
import { _settings } from './Settings.styled';

const Settings = () => {
  return (
    <_settings>
      <h1>Settings</h1>
      {/* auto signing */}
      {/* key export */}
      {/* airdrop */}
      {/* rpc connection url */}
      {/* Change game session */}
    </_settings>
  );
};

export default Settings;
